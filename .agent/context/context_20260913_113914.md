# Session Context Snapshot
**Saved:** 2026-09-13T11:39:00+07:00
**Branch:** feat/banc-custom-analytics-templates
**Last Commit:** b015224 merge: feat/auto-increment-mabnnc - regression tab, advisor system, docs update

## What Was Done This Session
1. **Thống kê SPSS & Biến Chuyển Tuyến:**
   - Bổ sung biến `chuyenTuyen` (Chuyển tuyến: Có/Không) vào cấu hình biến SPSS mặc định và logic trích xuất dữ liệu (`patientToSpssRow.ts`, `StepKetCuc.tsx`, `spssDefaultConfig.ts`).
2. **Tái Cấu Trúc Giao Diện Bảng Chéo & Hồi Quy:**
   - Phân chia thành 3 khối trực quan:
     - Khối 1: Quản lý & Lưu mẫu phân tích / hồi quy (Nền xanh dương nhạt).
     - Khối 2: Thiết lập biến phân tích (Nền vàng nhạt).
     - Khối 3: Kết quả & Diễn giải thống kê y khoa (Nền trắng tinh khiết).
   - Đưa tùy chọn đảo hàng/cột xuống trực tiếp thanh công cụ của bảng ở Khối 3.
   - Loại bỏ các checkbox tần số kỳ vọng và phần dư bị trùng lặp ở Khối 2.
3. **Sửa Lỗi & Tối Ưu Hóa Xem Trước BANC (Print & Preview):**
   - Khắc phục triệt để lỗi hiển thị nửa trang khi preview bản in BANC.
   - Hỗ trợ xem trước toàn trang A4 với thanh trượt thu phóng (Zoom: 80% - 150%) và bộ chuyển đổi mẫu chuẩn / bệnh nhân thực tế.
4. **Hộp Nhóm Biến BANC (Composite Variable Box) & Bật/Tắt Từng Biến Con:**
   - Triển khai cấu trúc `subFields` trong `bancFields.ts`.
   - Mỗi hộp nhóm biến (Diễn biến điều trị, Tình trạng ra viện, Kháng sinh, Sinh hiệu, CTM, CURB-65,...) đều có công tắc bật/tắt cả nhóm và nút **"Chi tiết" / "Thu gọn"**.
   - Khi bấm "Chi tiết", danh sách các biến con bung ra với công tắc toggle độc lập (ví dụ: trong Diễn biến điều trị, có thể tắt Thở máy, chỉ giữ Sốc NK và Lọc máu).
   - Đồng bộ hiển thị chuẩn xác trên bản in & file PDF BANC (`PrintResearchRecord.tsx`).
5. **Khắc Phục Lưu Mẫu Phân Tích Online Trên Firestore:**
   - Phát hiện nguyên nhân mẫu bị mất do `firestore.rules` chưa deploy lên Firebase Cloud khiến cloud chặn quyền ghi `analysis_templates`.
   - Đã deploy thành công `firestore.rules` lên Firebase Cloud.
   - Nâng cấp `templateService.ts` với cơ chế đồng bộ 2 chiều (Smart Merge) và tự động upload mẫu từ Local lên Firestore khi mở ứng dụng.
6. **Đồng Bộ Sổ Tay Người Dùng:**
   - Cập nhật đầy đủ `instruction.md` phản ánh mọi tính năng mới.

## Work In Progress
- Toàn bộ các tính năng yêu cầu trong phiên đều đã hoàn thành 100%, kiểm tra build và chạy thử trên browser đạt kết quả hoàn hảo.

## Key Files Modified
- `firestore.rules`: Cấp quyền read/write cho collection `analysis_templates`.
- `src/services/templateService.ts`: Quản lý lưu và đồng bộ 2 chiều mẫu phân tích Bảng chéo & Hồi quy.
- `src/data/bancFields.ts`: Định nghĩa danh mục biến BANC và các biến con (`subFields`).
- `src/pages/SettingsPage.tsx`: Giao diện tùy chỉnh biến BANC với nút Chi tiết/Thu gọn và switch con.
- `src/components/print/PrintResearchRecord.tsx`: Đồng bộ ẩn/hiện biến con trên bản in BANC.
- `src/components/analytics/CrosstabTab.tsx`: Tái cấu trúc 3 khối, thanh công cụ bảng chéo, lưu mẫu.
- `src/components/analytics/RegressionTab.tsx`: Tái cấu trúc 3 khối, lưu mẫu hồi quy.
- `src/utils/patientToSpssRow.ts`, `src/utils/spssDefaultConfig.ts`: Thêm biến chuyển tuyến vào SPSS.
- `instruction.md`: Cập nhật hướng dẫn sử dụng.

## Environment Notes
- Node: v25.2.1
- Firebase project: cap-research-app
- Target: Firebase Hosting & Firestore Asian Southeast 1

## Important Context for Next Session
- `firestore.rules` đã được deploy lên production.
- Collection `analysis_templates` đã hoạt động bình thường trên Firebase.
- Cấu hình hiển thị BANC được lưu trong `localStorage` key `cap_banc_field_visibility` và đồng bộ qua Firestore.
