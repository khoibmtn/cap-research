# Session Context Snapshot
**Saved:** 2026-08-12 03:25 (UTC+7)
**Branch:** feat/auto-increment-mabnnc
**Last Commit:** 57b88fb fix: SettingsPage advisor wrapper JSX + regression build errors

## What Was Done This Session

### 1. Advisor Account System
- Created `advisor@capresearch.com` (password: `123456`) — view-only role
- Role-based access control: advisor sees all pages but cannot edit/delete
- Settings page visible for advisor with `pointer-events-none` overlay + banner
- Dashboard buttons (Add/Backup/Edit/Delete/Toggle) hidden for advisor
- PatientFormPage edit/delete buttons hidden in view mode for advisor
- Login autocomplete: typing "advisor" → `advisor@capresearch.com`, "admin" → `admin@capresearch.com`

### 2. Password Management
- Password change modal for any logged-in user (in sidebar)
- Admin can reset advisor's password

### 3. Regression Analysis Tab (NEW)
- Added "Hồi quy" tab to Analytics page
- **Logistic Regression** (Newton-Raphson/IRLS): Omnibus, -2LL, Cox & Snell R², Nagelkerke R², Hosmer-Lemeshow, Wald, OR (95% CI), Classification Table
- **Linear Regression** (OLS): R², Adj R², ANOVA, Coefficients (B, β, t, p, 95% CI), VIF/Tolerance, Durbin-Watson
- Scatter plot + regression line + 95% CI band (univariate)
- Univariate / Multivariate mode toggle
- Dynamic variable picker with type validation (continuous vs binary)
- Auto-interpretation of results in Vietnamese

### 4. Bug Fixes
- Fixed SettingsPage advisor wrapper `<div>` JSX nesting (was closing in wrong function)
- Fixed merge conflicts between main and feature branch
- Fixed build errors (unused vars, Recharts Tooltip type)

## Work In Progress
- Advanced statistical interpretation for m×n crosstab tables (not started)
- Further PatientFormPage UX improvements for read-only state

## Key Files Modified
| File | Description |
|------|-------------|
| `src/utils/regressionEngine.ts` | **NEW** — OLS + logistic regression algorithms |
| `src/components/analytics/RegressionTab.tsx` | **NEW** — Full regression UI (1030+ lines) |
| `src/pages/AnalyticsPage.tsx` | Added regression tab integration |
| `src/pages/SettingsPage.tsx` | Fixed advisor wrapper JSX, read-only overlay |
| `src/components/layout/AppLayout.tsx` | Role badge, password change, advisor nav, timestamps |
| `src/App.tsx` | Settings route accessible to all (read-only for advisor) |
| `src/contexts/AuthContext.tsx` | Role derivation (admin/advisor) |
| `src/pages/PatientFormPage.tsx` | Edit/Delete hidden for advisor in view mode |

## Environment Notes
- Node: v25.2.1
- Firebase project: cap-research-app
- Last deploy: 2026-08-12 03:17 (UTC+7)
- Deploy URL: https://cap-research-app.web.app

## Important Context for Next Session
1. **Auth system**: `AuthContext.tsx` derives role from email domain — `admin@capresearch.com` = admin, `advisor@capresearch.com` = advisor. No Firebase Admin SDK (client-side only).
2. **Regression engine**: Pure TypeScript math — no external stats library. Uses Newton-Raphson for logistic, OLS normal equation for linear. All p-values via incomplete beta / gamma approximations.
3. **SPSS variable system**: Variables defined in `spssDefaultConfig.ts` with detailed coding (binary clinical vars: 0=Có, 1=Không for OR analysis). RegressionTab has its own variable registry extracted from Patient schema.
4. **Build gotcha**: `npm run build` = `tsc -b && vite build`. The `-b` (project build mode) is stricter than `--noEmit`. Always run full `npm run build` before deploy, not just `tsc --noEmit`.
5. **Deploy timestamp**: Always update sidebar timestamp in `AppLayout.tsx` line ~172 before deploy.
6. **Branch strategy**: Work on `feat/auto-increment-mabnnc`, merge to `main` for deploy, push main, switch back.
